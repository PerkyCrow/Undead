<!DOCTYPE html>
<html>
  <head>
    <%= render "shared/head" %>
  </head>
  <body>

    <div class="app">
      <header id="header">
        <%= render "shared/profile" %>
        <%= render "shared/logo" %>
      </header>

      <div class="main">
        <%= render "books/menu", book: @book %>

        <div class="content">
          <%= yield %>
        </div>
        <%= render "shared/footer" %>
      </div>
    </div>

  </body>
</html>
